macro_rules! macro_example {
    ($ident:ident: ($($tt:tt)*)) => {
        fn $ident {
            $($tt)*
        }
    }
}

macro_example! { main: (
    println!("This expands to regular rust code.");
)}
